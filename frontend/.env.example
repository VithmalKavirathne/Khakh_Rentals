# Base URL of the backend API (no trailing slash).
# Production (Vercel) -> your Render backend URL:
VITE_API_URL=https://your-backend.onrender.com

# Local development falls back to http://localhost:5000 if this is unset.
