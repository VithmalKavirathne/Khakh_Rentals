# Hostinger production — set before npm run build, or in hPanel Frontend Web App env vars
VITE_API_URL=https://api.yourdomain.com

# Local dev: leave unset — Vite proxies /api to http://localhost:5000
