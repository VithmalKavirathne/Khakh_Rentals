# Supabase + Hostinger — Frontend (build-time)
# Set these in Hostinger hPanel when deploying the Frontend Web App,
# or create frontend/.env.production locally before npm run build.

# Your Hostinger backend / API URL (no trailing slash)
VITE_API_URL=https://api.yourdomain.com
